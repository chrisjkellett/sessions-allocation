import React from 'react';
import classes from './Examiner.css';

const examiner = (props) => {
  return(
    <div className={classes.Examiner}>
      <h1>Examiner</h1>
    </div>
  )
}

export default examiner;